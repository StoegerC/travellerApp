Traveller Charsheet – Offene Aufgaben
======================================
Abgeschlossene Features siehe CHANGELOG.md

CODE-QUALITÄT
[ ] Tests für backend/authz.js + frontend/js/sync-merge.js (pure Funktionen,
    node --test ohne neue Abhängigkeit — dort lagen die letzten ernsten Bugs).
    Erste Test-Suite existiert bereits: backend/test/ (Auth-Härtung), gleiches
    Muster übernehmen.
[ ] notes.js aufteilen (2.274 Zeilen; Sub-Tabs als natürliche Schnittlinien),
    ship.js (1.548) beobachten

KAMPAGNE
[ ] Entfernen propagiert via Sync (entferntes Gerät erkennt Ausschluss beim Poll)

DATENHALTUNG
[ ] Selbst gehostetes Backend (Plan: ~/.claude/plans/lively-moseying-jellyfish.md) —
    Phase 1 läuft live: Server + SQLite + Endpunkt-Parität + Tailscale-Funnel-Tunnel +
    macOS-Autostart (launchd) stehen, echte Migration gegen den produktiven Worker
    gefahren, erstes Gerät umgestellt und Sync verifiziert. Noch offen: restliche Geräte
    umstellen, ein paar Tage beobachten, dann Cloudflare Worker/KV abschalten.
    Phase 2 (Datei-Uploads) und Phase 3 (Nutzerverwaltung/Login) sind umgesetzt, siehe
    CHANGELOG.md. Noch offen aus Phase 3: alle Geräte (iPhone, weitere Browser,
    GitHub-Pages-Zugriff) müssen sich einmalig neu mit E-Mail+Passwort anmelden, da der
    alte gemeinsame API-Key beim Cutover sofort ungültig wurde.
[ ] Charakter per QR-Code oder Link teilen (Base64-JSON)
[ ] Rollback (Admin-Seite, Snapshot wiederherstellen) sauberer gegen gleichzeitige
    Geräte-Pushes absichern — aktuell kann ein Gerät mit noch nicht synchronisierten
    lokalen Änderungen einen Rollback beim nächsten eigenen Speichern teilweise wieder
    rückgängig machen (Sync-Modell ist "lokal gewinnt bei Push-Konflikt"). Admin-UI weist
    nur mit einem Hinweistext darauf hin, löst es aber nicht strukturell.

CHARAKTERVERWALTUNG
[ ] Skill-Level -1 (nicht vorhanden) vs. 0 (Level 0) klar in der UI unterscheiden
[ ] Regel-System-Switch (character.system wird gesetzt aber nie ausgewertet)

UI / UX
[ ] Passwort selbst ändern im Cloud-Dialog (aktuell nur Admin-Reset möglich,
    der zudem das Erst-Login-Übernahmefenster wieder öffnet)
[ ] Sync-Fehler deutlicher machen: Warnung "Änderungen noch nicht hochgeladen"
    bzw. Zähler ausstehender Pushes statt nur dezentem Header-Badge
[ ] Erwähnungen aktualisieren: "@[Name](typ:id)" friert den Namen ein —
    Button "Erwähnungen aktualisieren" beim Umbenennen einer Person/eines Orts
[ ] Würfelwurf-Widget: 2d6 + Modifikator direkt in der App
[ ] Schnellsuche global im Header (Personen, Orte, Quests, Sessions)
[ ] Timeline-Ansicht: Sessions chronologisch als Zeitstrahl
[ ] Druckansicht / PDF-Export des Charakterbogens
[ ] Journal in Listenansicht
